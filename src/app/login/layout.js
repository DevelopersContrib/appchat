export const metadata = {
  title: 'Sign In — AppChat',
  description: 'Sign in to AppChat with your email. No password required — we use magic links.',
  robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
  return children;
}
